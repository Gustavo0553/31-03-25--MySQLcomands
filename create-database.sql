create database `gustavo`;
# a linha acima serve para criar um banco de dados com o nome: `Gustavo`
# o uso da crase (`) se torna necessário para indicar uma `Srting` do sistema
# `strings`do sistema: `database`, `table` e `campo`